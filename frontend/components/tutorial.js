import { Container } from 'react-bootstrap'; 

// This component will be nested inside another component (TODO) 
// where if user is not logged in or signed up, the tutorial will show up 
// on the home page 
export default function Tutorial() {
    return (
        <Container>

        </Container>
}