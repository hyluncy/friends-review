import Link from 'next/link'; 
import { Card } from 'react-bootstrap'; 

export default function ExperienceCard ({ name, category }) {
    return (
        <Link href={`/experiences/${id}`}>
            
        </Link>
    )
}